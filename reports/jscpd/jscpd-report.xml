<?xml version="1.0" encoding="UTF-8"?>
<pmd-cpd>
  <duplication lines="46">
    <file path="tools\build-pages-site.ts" line="161">
      <codefragment><![CDATA[    return 1;
}

/**
 * @param {readonly unknown[]} dictChildren
 *
 * @returns {ReadonlyMap<string, unknown>}
 */
function getDictionaryEntries(dictChildren) {
    /** @type {Map<string, unknown>} */
    const entries = new Map();
    /** @type {string | undefined} */
    let currentKey;

    for (const child of dictChildren) {
        if (hasOwnRecordKey(child, "key")) {
            currentKey = getTextNodeValue(child.key);
            continue;
        }

        if (currentKey !== undefined && !hasOwnRecordKey(child, "#text")) {
            entries.set(currentKey, child);
            currentKey = undefined;
        }
    }

    return entries;
}

/**
 * @param {ReadonlyMap<string, unknown>} entries
 * @param {string} key
 *
 * @returns {ReadonlyMap<string, unknown>}
 */
function getDictionaryValue(entries, key) {
    const value = entries.get(key);

    if (!isRecord(value)) {
        return new Map();
    }

    const dictChildren = getArrayProperty(value, "dict");
    return dictChildren === undefined
        ? new Map()
        : getDictionaryEntries(dictChildren);
}]]></codefragment>
    </file>
    <file path="tools\generate-theme-metadata.ts" line="213">
      <codefragment><![CDATA[    return color ?? null;
}

/**
 * @param {readonly unknown[]} dictChildren
 *
 * @returns {ReadonlyMap<string, unknown>}
 */
function getDictionaryEntries(dictChildren) {
    /** @type {Map<string, unknown>} */
    const entries = new Map();
    /** @type {string | undefined} */
    let currentKey;

    for (const child of dictChildren) {
        if (hasOwnRecordKey(child, "key")) {
            currentKey = getTextNodeValue(child.key);
            continue;
        }

        if (currentKey !== undefined && !hasOwnRecordKey(child, "#text")) {
            entries.set(currentKey, child);
            currentKey = undefined;
        }
    }

    return entries;
}

/**
 * @param {ReadonlyMap<string, unknown>} entries
 * @param {string} key
 *
 * @returns {ReadonlyMap<string, unknown>}
 */
function getDictionaryValue(entries, key) {
    const value = entries.get(key);

    if (!isRecord(value)) {
        return new Map();
    }

    const dictChildren = getArrayProperty(value, "dict");
    return dictChildren === undefined
        ? new Map()
        : getDictionaryEntries(dictChildren);
}]]></codefragment>
    </file>
    <codefragment><![CDATA[    return 1;
}

/**
 * @param {readonly unknown[]} dictChildren
 *
 * @returns {ReadonlyMap<string, unknown>}
 */
function getDictionaryEntries(dictChildren) {
    /** @type {Map<string, unknown>} */
    const entries = new Map();
    /** @type {string | undefined} */
    let currentKey;

    for (const child of dictChildren) {
        if (hasOwnRecordKey(child, "key")) {
            currentKey = getTextNodeValue(child.key);
            continue;
        }

        if (currentKey !== undefined && !hasOwnRecordKey(child, "#text")) {
            entries.set(currentKey, child);
            currentKey = undefined;
        }
    }

    return entries;
}

/**
 * @param {ReadonlyMap<string, unknown>} entries
 * @param {string} key
 *
 * @returns {ReadonlyMap<string, unknown>}
 */
function getDictionaryValue(entries, key) {
    const value = entries.get(key);

    if (!isRecord(value)) {
        return new Map();
    }

    const dictChildren = getArrayProperty(value, "dict");
    return dictChildren === undefined
        ? new Map()
        : getDictionaryEntries(dictChildren);
}]]></codefragment>
  </duplication>
  <duplication lines="11">
    <file path="tools\build-pages-site.ts" line="289">
      <codefragment><![CDATA[function getPreviewRules(settingsArray) {
    return settingsArray.flatMap((item) => {
        if (!hasOwnRecordKey(item, "dict")) {
            return [];
        }

        const dictChildren = getArrayProperty(item, "dict");
        const entries =
            dictChildren === undefined
                ? new Map()
                : getDictionaryEntries(dictChildren);
        const scope = getOptionalString(entries, "scope");]]></codefragment>
    </file>
    <file path="tools\generate-theme-metadata.ts" line="365">
      <codefragment><![CDATA[function getThemeScopes(settingsArray) {
    return settingsArray
        .flatMap((item) => {
            if (!hasOwnRecordKey(item, "dict")) {
                return [];
            }

            const dictChildren = getArrayProperty(item, "dict");
            const entries =
                dictChildren === undefined
                    ? new Map()
                    : getDictionaryEntries(dictChildren);
            const scope = getStringValue(entries.get("scope"));]]></codefragment>
    </file>
    <codefragment><![CDATA[function getPreviewRules(settingsArray) {
    return settingsArray.flatMap((item) => {
        if (!hasOwnRecordKey(item, "dict")) {
            return [];
        }

        const dictChildren = getArrayProperty(item, "dict");
        const entries =
            dictChildren === undefined
                ? new Map()
                : getDictionaryEntries(dictChildren);
        const scope = getOptionalString(entries, "scope");]]></codefragment>
  </duplication>
  <duplication lines="27">
    <file path="tools\build-pages-site.ts" line="358">
      <codefragment><![CDATA[    return value;
}

/**
 * @param {unknown} value
 *
 * @returns {string | undefined}
 */
function getTextNodeValue(value) {
    if (!isUnknownArray(value)) {
        return undefined;
    }

    const firstValue = value[0];
    if (!isRecord(firstValue)) {
        return undefined;
    }

    const textValue = firstValue["#text"];
    return typeof textValue === "string" ? textValue : undefined;
}

/**
 * @param {unknown} metadata
 *
 * @returns {readonly Record<string, unknown>[]}
 */
function getThemes(metadata) {]]></codefragment>
    </file>
    <file path="tools\generate-theme-metadata.ts" line="280">
      <codefragment><![CDATA[    return getTextNodeValue(value.string);
}

/**
 * @param {unknown} value
 *
 * @returns {string | undefined}
 */
function getTextNodeValue(value) {
    if (!isUnknownArray(value)) {
        return undefined;
    }

    const firstValue = value[0];
    if (!isRecord(firstValue)) {
        return undefined;
    }

    const textValue = firstValue["#text"];
    return typeof textValue === "string" ? textValue : undefined;
}

/**
 * @param {readonly unknown[]} settingsArray
 *
 * @returns {ThemeColors}
 */
function getThemeColors(settingsArray) {]]></codefragment>
    </file>
    <codefragment><![CDATA[    return value;
}

/**
 * @param {unknown} value
 *
 * @returns {string | undefined}
 */
function getTextNodeValue(value) {
    if (!isUnknownArray(value)) {
        return undefined;
    }

    const firstValue = value[0];
    if (!isRecord(firstValue)) {
        return undefined;
    }

    const textValue = firstValue["#text"];
    return typeof textValue === "string" ? textValue : undefined;
}

/**
 * @param {unknown} metadata
 *
 * @returns {readonly Record<string, unknown>[]}
 */
function getThemes(metadata) {]]></codefragment>
  </duplication>
  <duplication lines="20">
    <file path="tools\build-pages-site.ts" line="358">
      <codefragment><![CDATA[    return value;
}

/**
 * @param {unknown} value
 *
 * @returns {string | undefined}
 */
function getTextNodeValue(value) {
    if (!isUnknownArray(value)) {
        return undefined;
    }

    const firstValue = value[0];
    if (!isRecord(firstValue)) {
        return undefined;
    }

    const textValue = firstValue["#text"];
    return typeof textValue === "string" ? textValue : undefined;
}]]></codefragment>
    </file>
    <file path="tools\validate-themes.ts" line="102">
      <codefragment><![CDATA[    return isUnknownArray(value) ? value : undefined;
}

/**
 * @param {unknown} value
 *
 * @returns {string | undefined}
 */
function getTextNodeValue(value) {
    if (!isUnknownArray(value)) {
        return undefined;
    }

    const firstValue = value[0];
    if (!isRecord(firstValue)) {
        return undefined;
    }

    const textValue = firstValue["#text"];
    return typeof textValue === "string" ? textValue : undefined;
}]]></codefragment>
    </file>
    <codefragment><![CDATA[    return value;
}

/**
 * @param {unknown} value
 *
 * @returns {string | undefined}
 */
function getTextNodeValue(value) {
    if (!isUnknownArray(value)) {
        return undefined;
    }

    const firstValue = value[0];
    if (!isRecord(firstValue)) {
        return undefined;
    }

    const textValue = firstValue["#text"];
    return typeof textValue === "string" ? textValue : undefined;
}]]></codefragment>
  </duplication>
  <duplication lines="77">
    <file path="tools\build-pages-site.ts" line="405">
      <codefragment><![CDATA[    return getArrayProperty(settingsNode, "array") ?? [];
}

/**
 * @param {unknown} parsedDocument
 *
 * @returns {ReadonlyMap<string, unknown>}
 */
function getTopLevelDictionary(parsedDocument) {
    if (!isUnknownArray(parsedDocument)) {
        return new Map();
    }

    const plistNode = parsedDocument.find((node) =>
        hasOwnRecordKey(node, "plist")
    );

    if (!isRecord(plistNode)) {
        return new Map();
    }

    const plistChildren = getArrayProperty(plistNode, "plist");
    if (plistChildren === undefined) {
        return new Map();
    }

    const dictNode = plistChildren.find((node) =>
        hasOwnRecordKey(node, "dict")
    );

    if (!isRecord(dictNode)) {
        return new Map();
    }

    const dictChildren = getArrayProperty(dictNode, "dict");
    return dictChildren === undefined
        ? new Map()
        : getDictionaryEntries(dictChildren);
}

/**
 * @param {unknown} value
 * @param {string} key
 *
 * @returns {value is Record<string, unknown>}
 */
function hasOwnRecordKey(value, key) {
    return isRecord(value) && Object.hasOwn(value, key);
}

/**
 * @param {unknown} value
 *
 * @returns {value is Record<string, unknown>}
 */
function isRecord(value) {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}

/**
 * @param {unknown} value
 *
 * @returns {value is readonly unknown[]}
 */
function isUnknownArray(value) {
    return Array.isArray(value);
}

/**
 * @returns {Promise<number>}
 */
async function main() {
    if (shouldCheck === shouldWrite) {
        process.stderr.write("Specify exactly one of --check or --write.\n");
        return 1;
    }

    const siteData = await buildSiteData();]]></codefragment>
    </file>
    <file path="tools\generate-theme-metadata.ts" line="452">
      <codefragment><![CDATA[    };
}

/**
 * @param {unknown} parsedDocument
 *
 * @returns {ReadonlyMap<string, unknown>}
 */
function getTopLevelDictionary(parsedDocument) {
    if (!isUnknownArray(parsedDocument)) {
        return new Map();
    }

    const plistNode = parsedDocument.find((node) =>
        hasOwnRecordKey(node, "plist")
    );

    if (!isRecord(plistNode)) {
        return new Map();
    }

    const plistChildren = getArrayProperty(plistNode, "plist");
    if (plistChildren === undefined) {
        return new Map();
    }

    const dictNode = plistChildren.find((node) =>
        hasOwnRecordKey(node, "dict")
    );

    if (!isRecord(dictNode)) {
        return new Map();
    }

    const dictChildren = getArrayProperty(dictNode, "dict");
    return dictChildren === undefined
        ? new Map()
        : getDictionaryEntries(dictChildren);
}

/**
 * @param {unknown} value
 * @param {string} key
 *
 * @returns {value is Record<string, unknown>}
 */
function hasOwnRecordKey(value, key) {
    return isRecord(value) && Object.hasOwn(value, key);
}

/**
 * @param {unknown} value
 *
 * @returns {value is Record<string, unknown>}
 */
function isRecord(value) {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}

/**
 * @param {unknown} value
 *
 * @returns {value is readonly unknown[]}
 */
function isUnknownArray(value) {
    return Array.isArray(value);
}

/**
 * @returns {Promise<number>}
 */
async function main() {
    if (shouldCheck === shouldWrite) {
        process.stderr.write("Specify exactly one of --check or --write.\n");
        return 1;
    }

    const manifest = await buildManifest();]]></codefragment>
    </file>
    <codefragment><![CDATA[    return getArrayProperty(settingsNode, "array") ?? [];
}

/**
 * @param {unknown} parsedDocument
 *
 * @returns {ReadonlyMap<string, unknown>}
 */
function getTopLevelDictionary(parsedDocument) {
    if (!isUnknownArray(parsedDocument)) {
        return new Map();
    }

    const plistNode = parsedDocument.find((node) =>
        hasOwnRecordKey(node, "plist")
    );

    if (!isRecord(plistNode)) {
        return new Map();
    }

    const plistChildren = getArrayProperty(plistNode, "plist");
    if (plistChildren === undefined) {
        return new Map();
    }

    const dictNode = plistChildren.find((node) =>
        hasOwnRecordKey(node, "dict")
    );

    if (!isRecord(dictNode)) {
        return new Map();
    }

    const dictChildren = getArrayProperty(dictNode, "dict");
    return dictChildren === undefined
        ? new Map()
        : getDictionaryEntries(dictChildren);
}

/**
 * @param {unknown} value
 * @param {string} key
 *
 * @returns {value is Record<string, unknown>}
 */
function hasOwnRecordKey(value, key) {
    return isRecord(value) && Object.hasOwn(value, key);
}

/**
 * @param {unknown} value
 *
 * @returns {value is Record<string, unknown>}
 */
function isRecord(value) {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}

/**
 * @param {unknown} value
 *
 * @returns {value is readonly unknown[]}
 */
function isUnknownArray(value) {
    return Array.isArray(value);
}

/**
 * @returns {Promise<number>}
 */
async function main() {
    if (shouldCheck === shouldWrite) {
        process.stderr.write("Specify exactly one of --check or --write.\n");
        return 1;
    }

    const siteData = await buildSiteData();]]></codefragment>
  </duplication>
  <duplication lines="13">
    <file path="tools\build-pages-site.ts" line="422">
      <codefragment><![CDATA[    if (!isRecord(plistNode)) {
        return new Map();
    }

    const plistChildren = getArrayProperty(plistNode, "plist");
    if (plistChildren === undefined) {
        return new Map();
    }

    const dictNode = plistChildren.find((node) =>
        hasOwnRecordKey(node, "dict")
    );

    if (!isRecord(dictNode)) {]]></codefragment>
    </file>
    <file path="tools\validate-themes.ts" line="164">
      <codefragment><![CDATA[    if (plistNode === undefined) {
        return new Map();
    }

    const plistChildren = getArrayProperty(plistNode, "plist");
    if (plistChildren === undefined) {
        return new Map();
    }

    const dictNode = plistChildren.find((node) =>
        hasOwnRecordKey(node, "dict")
    );

    if (dictNode === undefined) {]]></codefragment>
    </file>
    <codefragment><![CDATA[    if (!isRecord(plistNode)) {
        return new Map();
    }

    const plistChildren = getArrayProperty(plistNode, "plist");
    if (plistChildren === undefined) {
        return new Map();
    }

    const dictNode = plistChildren.find((node) =>
        hasOwnRecordKey(node, "dict")
    );

    if (!isRecord(dictNode)) {]]></codefragment>
  </duplication>
  <duplication lines="34">
    <file path="tools\build-pages-site.ts" line="442">
      <codefragment><![CDATA[        : getDictionaryEntries(dictChildren);
}

/**
 * @param {unknown} value
 * @param {string} key
 *
 * @returns {value is Record<string, unknown>}
 */
function hasOwnRecordKey(value, key) {
    return isRecord(value) && Object.hasOwn(value, key);
}

/**
 * @param {unknown} value
 *
 * @returns {value is Record<string, unknown>}
 */
function isRecord(value) {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}

/**
 * @param {unknown} value
 *
 * @returns {value is readonly unknown[]}
 */
function isUnknownArray(value) {
    return Array.isArray(value);
}

/**
 * @returns {Promise<number>}
 */
async function main() {]]></codefragment>
    </file>
    <file path="tools\validate-themes.ts" line="204">
      <codefragment><![CDATA[    return entries;
}

/**
 * @param {unknown} value
 * @param {string} key
 *
 * @returns {value is Record<string, unknown>}
 */
function hasOwnRecordKey(value, key) {
    return isRecord(value) && Object.hasOwn(value, key);
}

/**
 * @param {unknown} value
 *
 * @returns {value is Record<string, unknown>}
 */
function isRecord(value) {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}

/**
 * @param {unknown} value
 *
 * @returns {value is readonly unknown[]}
 */
function isUnknownArray(value) {
    return Array.isArray(value);
}

/**
 * @returns {Promise<number>}
 */
async function main() {]]></codefragment>
    </file>
    <codefragment><![CDATA[        : getDictionaryEntries(dictChildren);
}

/**
 * @param {unknown} value
 * @param {string} key
 *
 * @returns {value is Record<string, unknown>}
 */
function hasOwnRecordKey(value, key) {
    return isRecord(value) && Object.hasOwn(value, key);
}

/**
 * @param {unknown} value
 *
 * @returns {value is Record<string, unknown>}
 */
function isRecord(value) {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}

/**
 * @param {unknown} value
 *
 * @returns {value is readonly unknown[]}
 */
function isUnknownArray(value) {
    return Array.isArray(value);
}

/**
 * @returns {Promise<number>}
 */
async function main() {]]></codefragment>
  </duplication>
  <duplication lines="28">
    <file path="tools\generate-theme-metadata.ts" line="337">
      <codefragment><![CDATA[    };
}

/**
 * @param {string} directory
 *
 * @returns {Promise<readonly string[]>}
 */
async function getThemeFiles(directory) {
    // eslint-disable-next-line security/detect-non-literal-fs-filename -- The generator intentionally reads the repo-local themes directory.
    const directoryEntries = await readdir(directory, { withFileTypes: true });

    return directoryEntries
        .filter(
            (directoryEntry) =>
                directoryEntry.isFile() &&
                directoryEntry.name.endsWith(".tmTheme")
        )
        .map((directoryEntry) => directoryEntry.name)
        .toSorted((left, right) => left.localeCompare(right))
        .map((fileName) => path.join(directory, fileName));
}

/**
 * @param {readonly unknown[]} settingsArray
 *
 * @returns {readonly string[]}
 */
function getThemeScopes(settingsArray) {]]></codefragment>
    </file>
    <file path="tools\validate-themes.ts" line="121">
      <codefragment><![CDATA[    return typeof textValue === "string" ? textValue : undefined;
}

/**
 * @param {unknown} value
 * @param {string} key
 *
 * @returns {string | undefined}
 */
/**
 * @param {string} directory
 *
 * @returns {Promise<readonly string[]>}
 */
async function getThemeFiles(directory) {
    // eslint-disable-next-line security/detect-non-literal-fs-filename -- The validator intentionally reads the repo-local themes directory.
    const directoryEntries = await readdir(directory, { withFileTypes: true });

    return directoryEntries
        .filter(
            (directoryEntry) =>
                directoryEntry.isFile() &&
                directoryEntry.name.endsWith(".tmTheme")
        )
        .map((directoryEntry) => directoryEntry.name)
        .toSorted((left, right) => left.localeCompare(right))
        .map((fileName) => path.join(directory, fileName));
}

/**
 * @param {unknown} parsedDocument
 *
 * @returns {ReadonlyMap<string, unknown>}
 */
function getTopLevelDictionary(parsedDocument) {]]></codefragment>
    </file>
    <codefragment><![CDATA[    };
}

/**
 * @param {string} directory
 *
 * @returns {Promise<readonly string[]>}
 */
async function getThemeFiles(directory) {
    // eslint-disable-next-line security/detect-non-literal-fs-filename -- The generator intentionally reads the repo-local themes directory.
    const directoryEntries = await readdir(directory, { withFileTypes: true });

    return directoryEntries
        .filter(
            (directoryEntry) =>
                directoryEntry.isFile() &&
                directoryEntry.name.endsWith(".tmTheme")
        )
        .map((directoryEntry) => directoryEntry.name)
        .toSorted((left, right) => left.localeCompare(right))
        .map((fileName) => path.join(directory, fileName));
}

/**
 * @param {readonly unknown[]} settingsArray
 *
 * @returns {readonly string[]}
 */
function getThemeScopes(settingsArray) {]]></codefragment>
  </duplication>
</pmd-cpd>