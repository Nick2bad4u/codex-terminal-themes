# Security

This repository stores local theme assets and validation tooling. It should not contain secrets, credentials, tokens, or machine-private configuration.

Report sensitive issues through the repository's private vulnerability reporting channel if one is enabled. Do not open public issues that disclose secrets or private local paths beyond the expected theme sync paths.
